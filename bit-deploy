
# Run the script to detect changes
../entria-deploy/bin/cli components

# Run the bit commands
# bit tag --all && bit export --eject

# Stage changes
# git checkout develop
# git add .

# 
# git commit -m "remove staged bit component and bump version"
